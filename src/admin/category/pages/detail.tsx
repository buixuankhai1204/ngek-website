import React from 'react';
import PropTypes from 'prop-types';

DetailCategory.propTypes = {
    
};

function DetailCategory(props: any) {
    return (
        <div>Detail Category</div>
    );
}

export default DetailCategory;